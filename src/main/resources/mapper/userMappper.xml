<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.iblog.dao.UserDao">
    <select id="select" parameterType="java.util.Map" resultType="com.iblog.entity.User">
        select
        user_id userId,
        nick_name nickName,
        password,
        user_authority userAuthority,
        crt_time crtTime,
        user_statu userStatu
        from tb_user
        <if test="nickName!=null and nickName!=''">
            where nick_name like "%"#{nickName}"%"
        </if>
    </select>
</mapper>